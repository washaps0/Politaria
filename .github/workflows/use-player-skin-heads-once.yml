name: Use player skin heads once

on:
  push:
    paths:
      - '.github/workflows/use-player-skin-heads-once.yml'
      - '.github/scripts/use_player_skin_heads.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch Skript GUIs
        run: python .github/scripts/use_player_skin_heads.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/plugins/Skript/scripts/countries.sk
          git add server/plugins/Skript/scripts/wanted.sk
          git commit -m "Use player skin heads in member and wanted lists"
          git push
